/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { useRef } from 'react'
import { useGLTF } from '@react-three/drei';
import { Canvas, useFrame, useThree } from '@react-three/fiber';

export default function Model({ ...props }) {
  const group = useRef();
  const { size } = useThree()
  const { nodes, materials } = useGLTF('/curation.glb')

  return (
    <group ref={group} {...props} dispose={null} scale={9}>
      <mesh  geometry={nodes.curation.geometry} material={materials['SVGMat.005']} />
    </group>
  )
}

useGLTF.preload('/curation.glb')
